public class PontosImportantes {
    //System.out.println: insere a saída na linha embaixo, o System.out.print apenas printa, na mesma linha
    //public class: É o modificador de acesso do método. Usando este modificador o método pode ser acessado por qualquer classe dentro (e fora) do projeto.
    //static: Define o método como estático, isso quer dizer que a classe não precisa ser instanciada para chamar este método.
    //void: É tipo de retorno do método. Este tipo de retorno significa vazio/nada, o método não dá retorno nenhum.
    //main: É o nome do método. 
    //(String [] args): Define que o método deve receber como parâmetro um array de String (nomeado args). Nesse caso específico: este parâmetro serve para caso seu programa precise receber algum valor como argumento.
    //concatenacao = 1+"1"+1+1; FICA 1111, pois quando detecta caractere não volta mais para operação
    //concatenacao = "1"+(1+1+1); FICA 13, por conta da evidência
    // Métodos tem sempre uma única função; ex: calcularImposto


}
